import 'package:flutter/material.dart';

class Archieved extends StatelessWidget {
  const Archieved({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Archieve screen"));
  }
}
